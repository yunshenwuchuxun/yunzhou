# 复制为 .env.local 用于本地开发；生产环境在 Vercel 控制台设置。
# 登录密码（单人使用）
APP_PASSWORD=change-me
# 会话 cookie 签名密钥，生产请用随机长串：openssl rand -hex 32
SESSION_SECRET=change-me-to-a-long-random-string
# 生产由 Vercel Postgres(Neon) 集成自动注入；本地留空则用 .data/state.json 兜底
# POSTGRES_URL=
